# سجل تعديل: تقرير الخزائن والسيولة وسجل التحويلات المالية بين الحسابات

* **التاريخ والوقت:** 2026-08-19 00:12
* **الدور المفعل:** Backend Architect & Frontend UI Agent
* **الهدف من التعديل:** بناء تقرير شامل ومفصل لحركة وأرصدة كافة الخزائن (درج الكاش، إنستاباي، المحافظ الذكية)، وعرض إجمالي السيولة المجمعة في كافة الخزائن (الكامل في الجميع)، واستعراض سجل التحويلات المالية بين الخزن مع العمولات وكشف حركة تسلسلي تفصيلي بالرصيد اللحظي ودعم الطباعة A4.

---

## 1. الملفات التي تم إنشاؤها أو تعديلها (Modified Files)
* `[MODIFIED]` `app/Services/TreasuryService.php` - إضافة دالتي `getTreasuryReport` و `buildLedgerEntries` لحساب رصيد أول المدة، وحركات الفترة، والتحويلات، وإجمالي السيولة، وكشف الحركة التسلسلي بدقة `DECIMAL(12,3)`.
* `[MODIFIED]` `app/Livewire/ReportsIndex.php` - إضافة التبويب السابع `treasury` وتمرير مصفوفة `treasuryData` للواجهة.
* `[MODIFIED]` `resources/views/livewire/reports-index.blade.php` - بناء واجهة تقرير الخزائن (كروت السيولة الحية، جدول مقارنة الخزائن، سجل التحويلات، وكشف الحركة اللحظي).
* `[MODIFIED]` `app/Http/Controllers/ReportPrintController.php` - إضافة دالة `printTreasuryReport` لتوليد تقرير رسمي A4 جاهز للطباعة والـ PDF.
* `[MODIFIED]` `resources/views/livewire/daily-journal-index.blade.php` - إضافة زر مباشر في اليومية للوصول السريع لتقرير الخزائن والسيولة.
* `[MODIFIED]` `app/Models/TreasuryTransfer.php` - إضافة علاقة `creator` كاسم مستعار لـ `user`.
* `[NEW]` `tests/Feature/TreasuryReportFeatureTest.php` - اختبارات آلية للتحقق من دقة الحسابات، التحويلات، واجهة Livewire، ومسار الطباعة A4.

---

## 2. القرارات المعمارية والمنطق البرمجي (Key Decisions)
* تطبيق حسابات دقيقة لكافة مقبوضات ومدفوعات وتحويلات كل خزينة على حدة بدوال `bcmath`.
* حساب رصيد أول المدة بدقة قبل تاريخ بداية الفترة لضمان سلامة الرصيد الافتتاحي.
* عرض كشف حركة تسلسلي (Running Ledger) يوضح الرصيد التراكمي بعد كل عملية (وارد/صادر/تحويل/مصروف).
* توفير طباعة رسمية A4 مخصصة للخزائن والسيولة مع التوتالات والإجماليات الشاملة.

---

## 3. الاختبارات والتحقق (Verification & Testing)
* [x] تم تشغيل كافة اختبارات النظام (135/135 اختبار ناجح بنسبة 100%).
* [x] تم التحقق من سلامة الواجهة باللغة العربية RTL والوضع الليلي Dark Mode.
* [x] تم اختبار أثر التحويلات بين الخزن وعمولات السحب بدقة متناهية.

---

## 4. الخطوات التالية المقترحة (Next Recommended Steps)
1. استكمال تحسينات الترجمة والمصطلحات المصرية العامة للنظام.
