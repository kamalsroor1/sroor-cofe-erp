# سجل تعديل: تحديث نظام ترقيم تشغيلات الـ Screenshots وسياسة التنظيف التلقائي
* **التاريخ والوقت:** 2026-08-21 20:23
* **الدور المفعل:** QA & Testing Specialist
* **الهدف:** ضبط هيكلة حفظ لقطات الشاشة بنظام تشغيلات متتابعة يومياً `[YYYY-MM-DD]/run-XX/` مع تطبيق سياسة تنظيف ذكية تلقائية تضمن الاحتفاظ بآخر 10 تشغيلات كحد أقصى يومياً، وآخر 3 أيام تاريخ فقط، مع ضمان اكتمال تحميل واجهات Vue 3 SPA بالكامل قبل التصوير.

## 1. الملفات المعدلة:
* `[MODIFIED]` `e2e/utils/screenshot-helper.js` - تطبيق الترقيم المتتابع `run-01`, `run-02` مع دوال `cleanupOldRuns` (بحد أقصى 10 تشغيلات باليوم) و `cleanupOldDays` (بحد أقصى 3 أيام).
* `[MODIFIED]` `e2e/utils/reporter-helper.js` - تضمين `runId` في تقارير `_report.json` و `_report.md`.
* `[MODIFIED]` `e2e-testing-log.md` - تحديث التوثيق وسياسة الـ Retention.

## 2. القرارات التقنية:
* **سياسة الحفظ التلقائي (Smart Auto-Retention):**
  - عند تشغيل رقم 11 في نفس اليوم، يتم حذف أقدم مجلد تشغيلة فوراً للحفاظ على مساحة التخزين.
  - عند إضافة مجلد يوم جديد يتجاوز 3 أيام، يتم حذف أقدم مجلد يوم بالكامل.
* **استراتيجية الانتظار (SPA Component Wait):**
  - الانتظار لـ `networkidle` ثم التحقق من بناء شجرة عناصر الـ Vue الحقيقية داخل `#app > *` قبل التقاط الصورة، مما منع التقاط أي شاشات فارغة أو داكنة وأنتج صوراً غنية بالبيانات والألوان تتراوح أحجامها بين 115 KB و 359 KB لكل شاشة.

## 3. التحقق والاختبار:
* [x] إنشاء مجلد `2026-08-21/run-01/` وتوليد الصور بداخله بنجاح.
* [x] فحص أحجام الصور والتأكد من خلوها 100% من الشاشات الفارغة أو السوداء.
* [x] استثناء كامل مجلدات الـ Screenshots من تتبع Git داخل `.gitignore`.
