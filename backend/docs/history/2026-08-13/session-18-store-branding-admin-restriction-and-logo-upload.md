# سجل تعديل: حصر إعدادات ترويسة وهُوِيّة الطباعة للمدير فقط وإضافة رفع وتغيير اللوجو

* **التاريخ والوقت:** 2026-08-13 22:42
* **الدور المفعل:** Backend Architect & Frontend UI
* **الهدف من التعديل:** قصر الوصول لبطاقة إعدادات ترويسة وهُوِيّة الطباعة (Store Branding & Logo) على حساب المدير العام فقط (`admin role`) ومنع ظهورها للموظفين العاديين، وإضافة ميزة رفع وتغيير صورة اللوجو (Logo Upload) مع المعاينة الحية.

---

## 1. التعديلات المنفذة (Changes Made)
* `[MODIFIED]` `resources/views/livewire/auth/profile.blade.php`:
  - إحاطة بطاقة الهوية والطباعة بشرط `@if(auth()->user()?->hasRole('admin'))`.
  - تغيير شارة البطاقة إلى `خاص بالمدير فقط`.
  - إضافة حقل اختيار ورفع صورة شعار جديدة مع ميزة المعاينة الحية قبل الحفظ (`temporaryUrl`).
* `[MODIFIED]` `app/Livewire/Auth/Profile.php`:
  - استخدام `Livewire\WithFileUploads`.
  - إضافة حماية برمجية `abort_if(!auth()->user()?->hasRole('admin'), 403)`.
  - معالجة وحفظ صورة اللوجو المرفوعة في `public_path('logo.png')` تلقائياً عند حفظ الإعدادات.

---

## 2. التحقق والتأكيد (Verification)
* [x] اجتياز 100/100 اختبار PHPUnit بنجاح (360 Assertion).
* [x] تم نشر التحديث وبناء كاشات الإنتاج على كافة نطاقات السيرفر بنجاح.
* [x] تم التحقق من حجب البطاقة عن المستخدمين غير المدراء.
