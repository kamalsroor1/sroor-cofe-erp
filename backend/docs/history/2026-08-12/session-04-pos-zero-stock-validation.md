# سجل تعديل: حظر إضافة الأصناف المنتهية أو غير المتوفرة بصفحة المبيعات مع إشعار SweetAlert أحمر

* **التاريخ والوقت:** 2026-08-12 16:37
* **الدور المفعل:** Backend Architect Agent & Frontend UI Agent
* **الهدف من التعديل:** منع إضافة أي صنف ليس له رصيد (0) أو تتجاوز الكمية المطلوبة الرصيد المتاح في نقطة البيع POS، وتنبيه المستخدم بـ SweetAlert أحمر دون إضافة المنتج للفاتورة.

---

## 1. الملفات التي تم إنشاؤها أو تعديلها (Modified Files)
* `[MODIFIED]` `d:\projects\sroor\app\Livewire\InvoiceCreate.php` - إضافة فحص حازم للرصيد المتاح بالمخزن قبل إضافة الصنف (`addItem`) أو تعديل الأوزان والجرامات (`setLineWeightPreset`, `setLineGrams`, `addLineWeightPreset`) وإلغاء الإضافة مع إطلاق تنبيه SweetAlert باللون الأحمر (`swal:toast` & `swal:alert`).
* `[MODIFIED]` `d:\projects\sroor\tests\Feature\LivewirePagesTest.php` - إضافة اختبارين جديدين للتحقق من منع إضافة الأصناف الصفرية وتجاوز الرصيد وتأكيد إطلاق التنبيهات باللون الأحمر.

---

## 2. القرارات المعمارية والمنطق البرمجي (Key Decisions)
* **الفحص الصارم للرصيد المتاح:** يتم حساب رصيد الصنف المتاح بالفرع الحالي من خلال `$item->getStockInStore($this->store_id)`.
* **الاستجابة الفورية بالتنبيه:** إذا كان الرصيد `0` أو كانت الكمية المطلوبة `> الرصيد المتاح`:
  1. يتم الخروج من الدالة فوراً عبر `return` لمنع إضافة البند إلى مصفوفة الفاتورة `$this->items`.
  2. إطلاق إشعار SweetAlert باللون الأحمر `swal:toast` برمز `icon: error`.
  3. إطلاق نافذة تحذيرية حمراء `swal:alert` بعنوان "نفاد الكمية بالمخزن!" أو "تجاوز رصيد المخزن!".

---

## 3. الاختبارات والتحقق (Verification & Testing)
* [x] تم تشغيل كافة اختبارات PHPUnit بنجاح كامل: **87 من أصل 87 اختبار ناجحة 100% (324 assertions)**.
* [x] تم التحقق من اختبارات منع إضافة الأصناف ذات الرصيد الصفري أو المتجاوزة للرصيد بنجاح.

---

## 4. الخطوات التالية المقترحة (Next Recommended Steps)
1. رفع التحديث للسيرفر وتأكيده للعميل.
