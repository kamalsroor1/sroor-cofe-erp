# قائمة المهام التفصيلية (Tasks Breakdown Checklist)

خطة تنفيذية مفككة لكافة مراحل المشروع بصيغة مهام قابلة للتتبع والتنفيذ (Checklist)، مقسمة حسب الطبقات الفنية: قاعدة البيانات، منطق الباك إند، واجهات Livewire و Blade، والاختبارات ومراقبة الجودة.

---

## المرحلة الأولى: النواة الأساسية للنظام (Phase 1: Core MVP)

### 1.1 قاعدة البيانات والنماذج (Database & Models)
- [x] إعداد مشروع Laravel وتثبيت الحزم الأساسية (Livewire 4, Spatie Permission).
- [x] إنشاء Migration جدول الأصناف `items` مع التأكيد الصارم على حقول `DECIMAL(12,3)` وتصنيف الأصناف `category`.
- [x] إنشاء Migration جدول العملاء `customers` مع حقل `current_balance DECIMAL(12,3)`.
- [x] إنشاء Migration جدول الموردين `suppliers` مع حقل `current_balance DECIMAL(12,3)`.
- [x] إنشاء Migration جدول فواتير المشتريات `purchases` وجدول بنودها `purchase_items`.
- [x] إنشاء Migration جدول فواتير المبيعات `invoices` وجدول بنودها `invoice_items` مع الفهارس والقيود الأجنبية.
- [x] إنشاء Migration جدول حركات المخزون الموحد `stock_movements`.
- [x] إنشاء Migration جدول إيداعات المخزون والتسويات `stock_deposits`.
- [x] إنشاء نماذج Eloquent (`Item`, `Customer`, `Supplier`, `Purchase`, `PurchaseItem`, `Invoice`, `InvoiceItem`, `StockMovement`, `StockDeposit`) وضبط العلاقات و Casts الرقمية.

### 1.2 منطق الأعمال والخدمات الخلفية (Backend Services)
- [x] بناء خدمة `StockService` للتحقق من الأرصدة، الصرف `stock_out`، التوريد `stock_in`، وتسجيل `StockMovement`.
- [x] بناء خدمة `PurchaseService` لإدارة فواتير الشراء، زيادة المخزون، وتحديث متوسط التكلفة وحسابات الموردين.
- [x] بناء خدمة `InvoiceService` لتنفيذ المعاملة المالية `DB::transaction()` وتطبيق القفل السطري `lockForUpdate()` على الأصناف.
- [x] برمجة دوال احتساب الخصومات المزدوجة (خصم السطر + خصم الفاتورة) واحتساب الصافي بدقة باستخدام `bcmath`.
- [x] منع البيع في حالة عدم كفاية المخزون ورمي استثناء مخصص وتراجع المعاملة (Rollback).

### 1.3 الواجهات ومكونات Livewire (Frontend & Livewire)
- [x] بناء قالب التخطيط العام `layouts/app.blade.php` متوافق مع Tailwind CSS و Flowbite و RTL.
- [x] بناء مكون Livewire لإدارة ودليل الأصناف `ItemList` مع البحث الفوري وحالات التفعيل والتعطيل.
- [x] بناء مكون نافذة سريعة لإضافة صنف `ItemQuickCreateModal`.
- [x] بناء مكون Livewire لإدارة العملاء `CustomerList` والموردين `SupplierList`.
- [x] بناء شاشة فواتير الشراء التفاعلية `PurchaseCreate` مع أزرار الأوزان الكبيرة وإظهار الكميات الموردة بوضوح.
- [x] بناء شاشة فواتير البيع الحية `InvoiceCreate` مع أزرار أوزان البن والشاي (ثمن، ربع، نصف، كيلو) والبحث السريع.

### 1.4 الاختبارات والجودة (Testing & QA)
- [x] كتابة اختبارات Unit Tests لحسابات الخصومات وصافي الفاتورة.
- [x] كتابة اختبارات Feature Tests لمعاملة اعتماد الفاتورة وتأكيد خصم المخزون وإنشاء سجل `StockMovement`.
- [x] اختبار القفل السطري `lockForUpdate()` ومحاكاة محاولة بيع نفس الرصيد المحدود من جلستين متزامنتين.

---

## المرحلة الثانية: المدفوعات والحسابات والمرتجعات (Phase 2: Payments, Ledgers & Returns)

### 2.1 قاعدة البيانات والنماذج (Database & Models)
- [x] إنشاء Migration جدول المدفوعات وسندات القبض والصرف `payments`.
- [x] إنشاء Migration جدول المرتجعات `returns` وجدول بنود المرتجعات `return_items`.
- [x] إنشاء نماذج Eloquent (`Payment`, `ReturnDocument`, `ReturnItem`) وضبط العلاقات مع الفواتير والعملاء والموردين.

### 2.2 منطق الأعمال والخدمات الخلفية (Backend Services)
- [x] بناء خدمة `PaymentService` لتسجيل الدفعات النقدية والمجزأة وتحديث حالة سداد الفاتورة (`paid`, `partially_paid`, `unpaid`).
- [x] بناء خدمة `CustomerBalanceService` لتحديث الرصيد التراكمي للعميل واستخراج بيانات كشف الحساب الزمني.
- [x] بناء خدمة `ReturnService` لمعالجة المرتجعات الجزئية وإعادة الكمية للمخزون وعكس الأثر المالي على العميل والمورد.
- [x] برمجة منطق إلغاء الفاتورة بالكامل (Cancellation) وعكس حركات المخزون والقيود المالية وتغيير الحالة إلى `cancelled`.

### 2.3 الواجهات ومكونات Livewire (Frontend & Livewire)
- [x] بناء مكون نافذة سند القبض `PaymentModal` لتسجيل دفعات العملاء وسداد الفواتير الآجلة.
- [x] بناء شاشة كشف حساب العميل التفاعلية `CustomerStatement` مع الفلترة بالتواريخ.
- [x] بناء شاشة كشف حساب المورد `SupplierStatement`.
- [x] بناء نافذة ومكون المرتجعات `ReturnCreate` و `ReturnIndex` لاختيار الأصناف وإدخال الكميات المرتجعة وسبب المرتجع.
- [x] إضافة خيار "إلغاء الفاتورة" للمستخدمين المصرح لهم مع تأكيد مسبب.

### 2.4 الاختبارات والجودة (Testing & QA)
- [x] اختبار عمليات السداد المتعدد لنفس الفاتورة والتحقق من حساب المتبقي وحالة السداد.
- [x] اختبار المرتجع الجزئي والتأكد من إضافة الكمية المرتجعة فقط إلى المخزن بدقة.
- [x] اختبار إلغاء الفاتورة والتأكد من عودة كامل كميات البنود للمخزن وتصفير رصيد الفاتورة دون حذفها.

---

## المرحلة الثالثة: التكاليف، الأرباح، ولوحة التحكم (Phase 3: Costing, Profits & Dashboard)

### 3.1 قاعدة البيانات والخدمات (Backend Logic & Profit Algorithms)
- [x] بناء خدمة `ProfitService` لحساب تكلفة البضاعة المباعة (COGS) بناءً على التكلفة المثبتة في أسطر الفاتورة.
- [x] تطبيق معادلات احتساب صافي الأرباح وهامش الربح للفاتورة، وللصنف، وللفترة الزمنية.
- [x] تفعيل وتحديث متوسط التكلفة المرجح (Weighted Average Cost) في كل عملية شراء جديدة.
- [x] بناء استعلامات إحصائيات لوحة التحكم المجمعة وسرعة الاستجابة.

### 3.2 الواجهات ومكونات Livewire (Frontend & Livewire)
- [x] بناء مكون إحصائيات لوحة التحكم `Dashboard` لعرض مبيعات اليوم، التحصيل، الديون المتأخرة، والأرباح.
- [x] بناء ويدجت تنبيهات النواقص `lowStockItems` لعرض الأصناف التي بلغت الحد الأدنى.
- [x] بناء شاشة التقارير والأرباح الشاملة `ReportsIndex` لتحليل ربحية كل صنف وقيمة بضاعة المخزن بالتكلفة وسعر البيع.

---

## المرحلة الرابعة: الطباعة، التصدير، وتطبيق الويب (Phase 4: Printing, Export & PWA)

### 4.1 قوالب الطباعة و CSS (Print Architecture)
- [x] تصميم وهيكلة قالب الطباعة القياسي `layouts/print-a4.blade.php` وضبط هوامش `@page` ودعم RTL.
- [x] تصميم وهيكلة قالب الطباعة الحراري `layouts/print-thermal.blade.php` المخصص لرول 80mm وإيصالات الكاشير.
- [x] إخفاء أشرطة القوائم والسايدبار والأزرار في ملفات الـ Print CSS واستدعاء `window.print()` بسلاسة.
- [x] بناء شاشات طباعة الفاتورة، سند القبض، وكشف الحساب لكلا القياسين.

### 4.2 التصدير وتطبيق الويب التفاعلي (Export & PWA)
- [x] بناء خدمة `ExportService` لتصدير كشف حساب العميل، كشف حساب المورد، وجرد المخزون لملفات Excel/CSV مع تضمين ترميز UTF-8 BOM لدعم اللغة العربية في إكسيل.
- [x] بناء ملف `manifest.json` و `sw.js` وربطهما لتحويل النظام إلى تطبيق ويب تقدمي (PWA) قابل للتثبيت على هواتف وأجهزة الكاشير المحمولة.

---

## المرحلة الخامسة: نظام الفروع وعربات التوزيع المتنقلة (Multi-Store & Wholesale Van Distribution)

### 5.1 البنية التحتية وقاعدة البيانات (Phase 1: Database & Migrations)
- [x] إنشاء جدول الفروع وعربات التوزيع والمخازن `stores`.
- [x] إنشاء جدول جرد وأسعار الفروع `store_stocks` مع دعم `custom_selling_price`.
- [x] إنشاء جدول أذونات تحويل البضاعة `stock_transfers` وبنودها `stock_transfer_items`.
- [x] إضافة `store_id` لكافة الجداول التشغيلية وترحيل السجلات القديمة تلقائياً إلى الفرع الرئيسي عبر `StoreSystemMigrationSeeder`.

### 5.2 طبقة الخدمات ومنطق التسعير الذكي (Phase 2: Services & Logic)
- [x] بناء خدمة التحويلات وشحن البضاعة `StockTransferService` مع القفل السطري `lockForUpdate()`.
- [x] تحديث `StockService` و `InvoiceService` و `PurchaseService` و `ReturnService` لتعمل على مستوى الفرع.
- [x] بناء خدمة ذاكرة الأسعار `CustomerPricingHelper` لاسترجاع آخر سعر بيع للعميل للصنف.
- [x] بناء وتفعيل `StoreScope` و `StoreAccess` لحماية صلاحيات الفروع والتبديل السريع.

### 5.3 الواجهات ونقطة البيع التفاعلية (Phase 3: UI & POS Experience)
- [x] بناء محوّل الفروع `StoreSwitcher` في الهيدر العلوي.
- [x] تطوير نقطة البيع `InvoiceCreate` لإظهار رصيد الفرع وشارة تذكّر آخر سعر للعميل بضغطة زر `[⚡ تطبيق السعر]`.
- [x] بناء شاشات إدارة الفروع `/stores` وتعيين المناديب.
- [x] بناء شاشة جرد وتسعير الفروع `/store-stocks`.
- [x] بناء شاشات أذونات التحويل والشحن `/stock-transfers` و `/stock-transfers/create`.

### 5.4 الورديات والتقارير المجمعة (Phase 4: Shifts & Consolidated Reports)
- [x] عزل ورديات الخزينة والصندوق لكل فرع / عربية توزيع في `ShiftService`.
- [x] فلترة اليومية وحركة الدرج بالفرع وتسوية عهدة العربية اليومية في `DailyJournalIndex`.
- [x] فلترة تقارير الأرباح وتكلفة المبيعات بالفرع وإضافة جدول مقارنة أداء ومبيعات الفروع وعربيات التوزيع في `ReportsIndex`.
- [x] حل مشكلة إضافة العميل والرصيد الافتتاحي وتأكيد خلو النظام من أي أخطاء 500.

---

## المرحلة الثامنة: السايدبار المصغر وشاشة نقطة البيع للمس (Phase 8: Collapsible Mini-Sidebar & Touch POS)
- [x] تحويل القائمة الجانبية (Sidebar) إلى قائمة ذكية قابلة للتصغير (`w-20` إلى `w-72`) مع زر تحكم وحفظ التفضيل في المتصفح.
- [x] بناء نوافذ عائمة (Hover Tooltips / Flyouts) أنيقة باللغة العربية تظهر عند التمرير بالماوس في الوضع المصغر.
- [x] تفعيل التصغير التلقائي والإجباري للسايدبار في صفحة نقطة البيع (`/invoices/create`) لتوفير أقصى مساحة للكاشير.
- [x] إعادة تصميم وهندسة شاشة المبيعات (`InvoiceCreate`) لتكون مهيأة بالكامل للمس بالأصابع (أهداف لمس واسعة `48px-56px`).
- [x] إضافة بطاقات أصناف مكبرة للمس السريع مع أزرار أوزان سريعة (`1/8`, `1/4`, `1/2`, `1كجم`) وأزرار كميات `[+]` و `[-]` في السلة.
- [x] إضافة لوحة أرقام لمس تفاعلية (Touch Keypad) وأزرار سداد نقدي سريع (`المبلغ بالضبط`, `50`, `100`, `200`).
- [x] إيقاف رابط خلاط التوليفات مؤقتاً بالتعليق دون حذفه.
- [x] اجتياز 100/100 اختبار PHPUnit واختبارات Playwright الحية على السيرفر بنجاح 100%.

---

## المرحلة التاسعة: البحث وتسجيل العملاء السريع وتنظيف قوالب الطباعة (Phase 9: Quick Customer POS & Clean Print Templates)
- [x] تفعيل البحث اللحظي عن العملاء بالاسم ورقم الهاتف في شاشة نقطة البيع POS مع قائمة منسدلة ذكية.
- [x] بناء نافذة تسجيل عميل جديد فورياً (`➕ عميل جديد`) من داخل شاشة POS مع الاختيار التلقائي له فور الحفظ.
- [x] تنظيف وتحديث قالب الطباعة الحراري (Thermal Receipt) بإزالة تفاصيل الفرع واسم الكاشير/البائع والاحتفاظ برقم الفاتورة الدال على الفرع.
- [x] تنظيف وتحديث قالب الطباعة العادي (A4 Invoice) بإزالة تفاصيل الفرع وبيانات البائع وتبسيط بطاقة العميل.
- [x] اجتياز كافة اختبارات الـ Unit & Feature Tests بنسبة 100/100 اختبار.
- [x] الرفع والمزامنة الكاملة على السيرفر السحابي والإنتاج.

---

## المرحلة العاشرة: إلغاء المشتريات، طرق الدفع الذكية، التسوية الجردية، كارت حركة الصنف، وطباعة التقارير A4
- [x] **إلغاء واستعادة فواتير الشراء وعكس المخزون وحساب المورد:**
  - إضافة `cancelPurchase()` في `PurchaseService` مع شرط كفاية الرصيد لمنع المخزون السالب، وعكس رصيد المخزن بحركة `purchase_cancel_out`، وإلغاء سندات الصرف، وتحديث رصيد المورد.
  - إضافة `restorePurchase()` لإعادة توريد البضاعة للمخزن وتحديث متوسط التكلفة المرجح WAC وتحديث حساب المورد.
  - تحديث واجهة `PurchaseIndex` بفلاتر الحالات (معتمدة، ملغاة، الكل) وأزرار الإلغاء والاستعادة.
- [x] **توسيع طرق الدفع وإيقاف الطرق غير المستخدمة (بدون حذف):**
  - إنشاء `PaymentMethod` Enum مع شارات أيقونات وألوان وتحديد الطرق النشطة (`activeMethods`: كاش، إنستاباي، محفظة ذكية).
  - إضافة حقل `payment_method` إلى جدول `invoices` وتحديث جدول `payments`.
  - إضافة أزرار الدفع السريع اللمسية في شاشة POS (`InvoiceCreate`) للطرق النشطة الثلاث.
  - تفعيل فلتر وسيلة الدفع في جداول الفواتير والمصروفات، وتحديث نوافذ سندات القبض والصرف للعملاء والموردين.
  - إيقاف الفيزا والشيك والتحويل البنكي من الاختيار في الواجهات مع الاحتفاظ بها في الـ Enum لحفظ سجل المعاملات التاريخية.
- [x] **التسوية الجردية وتصحيح رصيد المخزون (Stock Adjustment):**
  - إضافة `adjustStock()` في `StockService` بالقفل السطري الذري وحساب فوارق العجز والزيادة وتسجيل حركات `stock_adjustment_in` / `stock_adjustment_out`.
  - إضافة نافذة التسوية الجردية السريعة في دليل الأصناف (`ItemIndex`) مع محتسب الفارق اللحظي.
- [x] **كارت حركة الصنف (Stock Card) والفلترة والطباعة والتصدير:**
  - بناء مكون `ItemMovements` بمسار `/items/{id}/movements` مع فلاتر الفترات الزمنية (اليوم، الأسبوع، الشهر، العام، كل المدة)، الفروع، ونوع الحركة.
  - بناء قالب الطباعة A4 المعتمد `print-item-movements-a4.blade.php` مع إجماليات الوارد والمنصرف والرصيد الحالي ومربعات التوقيع.
  - بناء دالة التصدير `exportItemMovements()` بترميز UTF-8 BOM لدعم إكسيل بالعربية.
- [x] **طباعة كافة التقارير المالية A4 بشكل منظم:**
  - بناء متحكم `ReportPrintController` لتجهيز بيانات الطباعة للتبويبات الـ 6 (المبيعات، ربحية الأصناف، أداء الفروع، حسابات العملاء، المصروفات، جرد وتقييم المخزون).
  - بناء قالب `print-report-a4.blade.php` بهوية رسمية ورأس تقرير واضح ومؤشرات ومجاميع ومربعات اعتماد.
  - ربط زر "🖨️ طباعة التقرير A4" في هيدر صفحة `/reports`.

---

## المرحلة الحادية عشرة: منظومة المصاريف الإضافية الداينمك وتوزيع التكلفة (Dynamic Landed Costs & Multi-Expenses Breakdown)
- [x] **بناء جدول وموديل المصاريف الإضافية متعددة الأسطر (`additional_expenses` & `AdditionalExpense`):**
  - جدول متعدد الأغراض مرتبط بالعلاقة البوليمورفية (`document_type`, `document_id`) مع فواتير المشتريات وفواتير المبيعات.
  - دعم إضافة عدد غير محدود من أسطر المصاريف لكل فاتورة (شحن، عتالة، تغليف، جمارك، نولون، إكراميات).
  - تخزين طريقة التوزيع (`allocation_method`: حسب الكمية / الوزن `by_quantity`، حسب قيمة الصنف `by_value`، أو بالتساوي `equal`).
  - تخزين طريقة السداد (`paid_by`: مضاف لحساب المورد / العميل بالفاتورة، أو مدفوع نقداً/إنستاباي/محفظة من الخزينة بسند صرف مستقل `payment_id`).
- [x] **توزيع المصاريف على تكلفة الأصناف وتحديث WAC في المشتريات (`PurchaseService`):**
  - توزيع المصاريف تلقائياً على كل صنف وحساب تكلفة الوحدة المحملة `landed_unit_cost = base_cost + (allocated_expense / qty)`.
  - تحديث متوسط التكلفة المرجح `weighted_avg_cost` للأصناف بناءً على التكلفة المحملة لضمان دقة تقييم المخزون.
  - إنشاء سندات صرف خزينة تلقائية `Payment` للمصاريف المسددة من الصندوق أو الحسابات البنكية.
- [x] **واجهات المستخدم التفاعلية (`PurchaseCreate` & `PurchaseIndex`):**
  - إضافة جدول ديناميكي لإضافة وحذف بنود المصاريف بأزرار جاهزة (🚚 شحن، 👷 عتالة، 📦 تغليف، 📑 جمارك).
  - محتسب فوري مباشر للتكلفة المحملة مقارنة بالتكلفة الأساسية في أسطر الأصناف.
  - نافذة عرض تفاصيل الفاتورة والمصاريف الشاملة في قائمة فواتير الشراء (`PurchaseIndex`).
- [x] **المصاريف الإضافية والشحن في المبيعات (`InvoiceService` & `InvoiceCreate` & `InvoiceShow`):**
  - دعم مصاريف الشحن والتوصيل في شاشة نقطة البيع POS وحساب الصافي.
  - عرض تفاصيل المصاريف والشحن في شاشات التفاصيل فقط وعدم إدراج تفاصيلها في قوالب الطباعة للحفاظ على بساطة الفاتورة.
- [x] **الاختبارات الآلية الشاملة (`LandedCostAndAdditionalExpensesFeatureTest`):**
  - 6 اختبارات آلية ناجحة تغطي توزيع التكلفة بالوزن وبالقيمة، وسندات الخزينة، وإلغاء الفواتير، ومبيعات التوصيل.
  - اجتياز 125/125 اختبار بنجاح 100%.

---

## المرحلة الثانية عشرة: التحويل بين الخزن وحسابات الدفع والترجمة المصرية العامة (Treasury Transfers & Localization)
- [x] **بناء جدول وموديل التحويلات بين الخزن (`treasury_transfers` & `TreasuryTransfer`):**
  - جدول وسجل رسمي لحركات التحويل المالي بين وسائل الدفع المختلفة (كاش، إنستاباي، محافظ ذكية).
  - تسجيل المبلغ المحول `amount`، عمولة السحب/التحويل `transfer_fee`، والمستخدم والفرع والبيان.
- [x] **بناء خدمة إدارة الخزينة وحساب الأرصدة اللحظية (`TreasuryService`):**
  - استخراج الأرصدة الفعلية اللحظية لدرج الكاش، إنستاباي، والمحفظة، وإجمالي السيولة النقدية المتاحة.
  - تنفيذ عمليات التحويل المالي ذرياً داخل `DB::transaction()` مع تدقيق القيود وتسجيل النشاط.
- [x] **واجهة دفتر اليومية وتحويل الخزن (`DailyJournalIndex` & `daily-journal-index.blade.php`):**
  - إضافة بطاقات حية بأرصدة كل خزينة ومجموع السيولة الشاملة في هيدر اليومية.
  - إضافة زرار ونافذة منبثقة تفاعلية للتحويل بين الخزن مع إظهار رصيد كل حساب في القائمة المنسدلة.
  - إضافة تبويب وجدول مستقل لسجل التحويلات اليومية بين الخزن.
- [x] **ملفات الترجمة والأسلوب المصري التجاري العام (`lang/ar/`):**
  - إنشاء `lang/ar/treasury.php` و `lang/ar/expenses.php`.
  - تحسين مسميات الواجهات لتكون عامة وسهلة الفهم لأي تاجر أو كاشير (مين اللي هيدفع/هيتحمل المصروف؟ - درج النقدية - حساب إنستاباي - المحفظة الذكية).
- [x] **الاختبارات الآلية الشاملة (`TreasuryTransferFeatureTest`):**
  - 6 اختبارات آلية ناجحة للتحويلات وعمولات السحب وفحص كفاية رصيد الحساب المحول منه وتحديث الأرصدة.
  - اجتياز 131/131 اختبار بنجاح 100%.

---

## المرحلة الثالثة عشرة: التعميم الشامل لمصطلحات النظام والترجمة المصرية المعيارية (Full Egyptian Trade Localization)
- [x] **بناء ملفات الترجمة النموذجية في `lang/ar/`:**
  - `lang/ar/common.php` (المصطلحات العامة، الأزرار، الحالات، البيانات المشتركة).
  - `lang/ar/pos.php` (نقطة البيع، الكاشير، أنواع الدفع، الفكة، الزبائن).
  - `lang/ar/purchases.php` (المشتريات، التوريد، التكلفة المحملة، الشحن).
  - `lang/ar/inventory.php` (المخزون، الأصناف، حد الطلب، النواقص، التحويلات).
  - `lang/ar/contacts.php` (العملاء، الموردين، كشوف الحساب، سندات القبض والصرف).
  - `lang/ar/reports.php` (التقارير، الإحصائيات، الأرباح، السيولة).
  - تحديث `lang/ar.json` لدعم كافة المسميات المصرية.
- [x] **تعميم واجهات النظام وإلغاء التخصيص الحصري:**
  - شاشة POS: تغيير العنوان إلى "نقطة البيع والكاشير السريع (Touch POS)" وتوليد أزرار الأقسام ديناميكياً من قاعدة البيانات.
  - شاشة المشتريات والتوريد: تعميم نصوص التوريد ومربعات البحث.
  - شاشة تجميع الأصناف المركبة: تعميم موديول التصنيع والتجميع لخدمة أي نشاط تجاري.
  - قوالب الطباعة والهيدر: ربط المسميات ديناميكياً بجدول الإعدادات `Setting`.
- [x] **الاختبارات الآلية والتحقق الشامل:**
  - تحديث تأكيدات الاختبارات في `LivewirePagesTest`.
  - اجتياز 131/131 اختبار في PHPUnit بنجاح 100%.
  - الرفع على مستودع Git والنشر الآمن على السيرفر المباشر بنجاح.

---

## المرحلة الرابعة عشرة: الحزمة الخماسية المتقدمة للتحليلات والمشتريات الذكية (5 Selected Analytics & Intelligence Features)
- [x] **تحليل حركة البضاعة والأرباح (ABC Inventory Analysis):**
  - بناء خدمة `InventoryAnalyticsService` لتصنيف الأصناف إلى فئات (A 80%, B 15%, C 5%) وتحديد سرعة السحب والركود.
  - إضافة تبويب متكامل في تقارير النظام مع بطاقات الملخص وجدول تفاعلي وتنبيه البضاعة الراكدة.
  - دعم تصدير تقرير ABC إلى Excel / CSV مرمز بـ UTF-8 BOM.
  - دعم طباعة تقرير ABC بنموذج A4 رسمي.
- [x] **مراكز التكلفة وقائمة أرباح وخسائر الفروع وعربات التوزيع (Branch P&L & Cost Centers):**
  - إضافة حقل `cost_center` لجدول المصروفات وتصنيف المصروفات (تشغيل، إيجار، مرافق، رواتب، سيارات، صيانة، شحن).
  - بناء خدمة `ProfitLossService` لحساب قائمة الدخل وصافي أرباح كل فرع وعربة توزيع.
  - إضافة تبويب مقارنة الفروع ومراكز التكلفة في شاشة التقارير مع إمكانية طباعة تقرير الدخل A4.
- [x] **لوحة المؤشرات التفاعلية ومخطط ساعات الذروة (Interactive Analytics Dashboard):**
  - بناء خدمة `DashboardAnalyticsService` لحساب متوسط سلة الشراء ومخطط المبيعات اليومية.
  - إضافة مخطط حراري تفاعلي كامل للـ 24 ساعة يوضح كثافة العمليات لكل ساعة مع تلميحات بالماوس.
  - إضافة شريط توزيع طرق التحصيل والدفع في لوحة التحكم.
- [x] **مساعد المشتريات الذكي والتنبؤ بالنواقص (Smart Reorder Assistant):**
  - بناء خدمة `ReorderAssistantService` لحساب معدل الاستهلاك اليومي والمدة المتبقية للنفاد والكمية المقترحة.
  - شاشة متكاملة للمساعد `/purchases/smart-reorder` مع فلاتر الاستعجال وتحديد الأصناف.
  - نقل وتعبئة الكميات المقترحة تلقائياً بدقة إلى فاتورة الشراء والتوريد.
- [x] **التعريب واللهجة المصرية العامة وقواميس الترجمة:**
  - استكمال كافة ملفات `lang/ar/` بمصطلحات تجارية مبسطة وواضحة.

---

## المرحلة الخامسة عشرة: الأمان والرقابة والموبايل وتخصيص الفواتير (Audit Trail, Mobile UX & Print Builder)
- [x] **سجل النشاط والرقابة والتدقيق (Activity Log & Audit Trail):**
  - مراجعة وتأكيد تسجيل وتدقيق العمليات في كافة الخدمات (`InvoiceService`, `PurchaseService`, `StockTransferService`, `ReturnService`, `TreasuryService`, `PaymentService`, `ExpenseIndex`).
  - دعم التايم لاين والجدول والتصفية حسب المستخدم والفرع والعملية في شاشة سجل الرقابة `/activity-logs`.
- [x] **مركز الإشعارات والتنبيهات المباشر (In-App Notification Center):**
  - إضافة جرس إشعارات حي وتفاعلي في الهيدر مع شارة العداد المتحركة.
  - تنبيهات فورية للنواقص، المستحقات الآجلة للتحصيل، والسيولة النقدية العالية بالدرج.
- [x] **محرر قوالب الفواتير والطباعة المخصصة (Invoice Template Customizer):**
  - إضافة إعدادات التحكم بنص التذييل (Footer Note)، إظهار/إخفاء رصيد العميل السابق، ورمز الـ QR في الفاتورة الحرارية.
- [x] **التخزين المؤقت الذكي للأداء (15-Min Smart Report Caching):**
  - تفعيل التخزين المؤقت لتقارير ABC و P&L وتسريع الاستجابة على بيئة الإنتاج.
- [x] **الاختبارات الآلية الشاملة والتوافق:**
  - اجتياز 140+ اختبار آلي بنجاح 100%.


